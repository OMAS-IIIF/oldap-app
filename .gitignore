# Node.js dependenciestest-results
node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.vscode/ # Editor-specific settings (optional)

# OS-specific files
.DS_Store # macOS
Thumbs.db # Windows
*.swp      # Swap files (e.g., vim)

# SvelteKit specific
*.tsbuildinfo # TypeScript build info files
*.lock # Lock files (if you don't want to commit lock files, but this is optional)

# Optional: Ignore package-lock.json or yarn.lock if not committing lockfiles
package-lock.json
yarn.lock
pnpm-lock.yaml
/.idea
